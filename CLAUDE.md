# INMYIN

내가 가진 것을 사진으로 찍어 가상의 창고에 넣어두고, 한눈에 보고 남에게 보여주는 앱.

## 이 문서를 읽는 순서

1. `CLAUDE.md` (이 파일) — 프로젝트 규칙, 용어, 폴더 구조
2. `docs/PRD.md` — 무엇을 왜 만드는가
3. `docs/screens.md` — 화면 30개 상세 정의
4. `docs/data-model.md` — DB 스키마와 불변 규칙
5. `docs/roadmap.md` — 단계별 작업 목록

사람이 읽는 문서 (코드 작업에는 안 쓰임):

- `docs/for-beginners.md` — 용어 풀이, 배포·앱스토어 설명
- `docs/how-to-ask.md` — 클로드 코드에게 일 시키는 법
- `docs/figma-cleanup.md` — 피그마 정리 가이드

작업을 시작하기 전에 `docs/roadmap.md`에서 지금 단계가 어디인지 확인할 것.

---

## 기술 스택

| 영역 | 선택 | 이게 뭐냐면 |
| --- | --- | --- |
| 프레임워크 | Next.js (App Router) + TypeScript | 웹사이트의 뼈대. 주소 연결·서버·최적화를 담당 |
| 스타일 | Tailwind CSS | 색·여백을 짧은 이름으로 쓰는 방식 |
| 백엔드 | Supabase (Auth, Postgres, Storage) | 로그인·데이터·이미지 저장을 한 번에 빌려줌 |
| PWA | Serwist (manifest + 서비스워커) | 폰 홈 화면에 앱처럼 설치되게 하는 것 |
| 캔버스 | Konva.js (`react-konva`) — INMYIN 에디터 전용. 브라우저에서만 불러온다 | 그림을 올리고 돌리고 키우는 도구 |
| 배경제거 | `@imgly/background-removal` (브라우저 처리) | 사진에서 물건만 남기고 배경을 지움 |
| 배포 | Vercel | 만든 걸 인터넷에 올려주는 곳 |

**GraphQL, Prisma, 별도 API 서버는 쓰지 않는다.** Supabase 클라이언트로 직접 읽고 쓴다. 이유는 `docs/for-beginners.md` 참고.

### 서버는 DB 옆에 둔다

Supabase 프로젝트가 **서울(ap-northeast-2)** 에 있어서, Vercel 의 서버 코드도 서울(`icn1`)에서 돌린다 — `vercel.json` 의 `regions`. 이 설정이 없으면 서버가 미국(iad1)에서 돌면서 DB에 물어볼 때마다 태평양을 왕복하고, 화면 하나에 몇 초씩 걸린다. Supabase 위치를 옮기면 이것도 같이 바꾼다.

누르면 바로 반응해야 하는 동작(삭제 · 되돌리기 등)은 서버의 답을 기다리지 말고 화면부터 바꾼다.

### 배경제거는 반드시 함수 하나로 감쌀 것

```ts
// lib/background-removal.ts
export async function removeBackground(file: File): Promise<Blob>
```

나중에 외부 API로 갈아탈 수 있어야 한다. 이 함수 밖에서 배경제거 라이브러리를 직접 부르지 말 것.

`@imgly/background-removal`은 **AGPL-3.0** 이다. 남에게 서비스를 공개하기 전(2단계)에 어떻게 할지 정해야 한다 — `docs/roadmap.md`의 "나중에 할 일".

---

## 용어 (코드에서 이 이름을 그대로 쓴다)

| 한글 | 코드 | 정의 |
| --- | --- | --- |
| 인벤토리 | `Inventory` | 아이템을 담는 격자 판 |
| 칸 | `slot` | 인벤토리 안의 한 자리 |
| 아이템 | `Item` | 사진 + 이름 + 설명으로 등록된 물건 하나 |
| INMYIN | `InmyinPost` | 아이템을 캔버스에 배치해 만든 이미지 게시물 |
| 짐싸기 | `packing` | 인벤토리 두 개를 동시에 열어 아이템을 옮기는 모드 |
| 북마크 | `Bookmark` | 나중에 보려고 모아 두는 것. 책갈피 버튼. Bookmark 탭에 모이고 플랜 한도가 있다. 인기 순위도 북마크 수로 센다 |
| 하트 | `Heart` | 게시물에 반응해 주는 것. 한 사람이 한 게시물에 한 번. 수만 쌓이고 모아 보는 곳은 없다 |
| 댓글 | `Comment` | 게시물에 다는 글. 답글은 없다 |
| 알림 | `Notification` | 팔로우 · 하트 · 댓글이 생기면 받는 사람에게 쌓인다. 공지도 여기 |
| 팔로우 | `Follow` | 다른 유저 구독 |

표기 규칙:

- UI와 코드 모두 **INMYIN** (영문 대문자). "인마인"으로 쓰지 않는다
- "좋아요"라는 말은 쓰지 않는다 — 모으는 것은 **북마크**, 반응은 **하트**로 갈라 부른다. (처음엔 좋아요 하나가 둘을 겸했고, 친구와 써 본 뒤 갈랐다)
- 하단 탭은 **Bookmark · Home · My** 세 개. 화면에서도 이 순서다 — Home이 가운데
- 게시물 상세의 제목 줄 오른쪽은 **하트 아이콘 · 하트 수 · 댓글 아이콘 · 댓글 수 · 북마크 아이콘** 순서. 북마크 수는 보여주지 않는다

---

## 절대 어기면 안 되는 규칙

1. **아이템은 두 곳에 동시에 존재할 수 없다.** 실제 물건이기 때문이다. 이미 등록된 아이템을 다른 인벤토리로 "불러오기"하는 기능은 없다. 옮기려면 짐싸기를 쓴다 (= 부모를 바꾸는 것, 복사가 아니다)
2. **인벤토리는 다른 인벤토리 안에 담을 수 있다.** 단, 이미 어딘가에 들어가 있는 인벤토리는 또 담을 수 없다 — 인벤토리도 실제 물건이라 부모는 항상 하나다. 한 줄로 이어지는 중첩은 **5겹까지** (예: 집 › 방 › 가방 › 작은 가방 › 파우치)
3. **새 아이템은 기존 아이템을 밀어내지 않는다.** 항상 마지막 아이템의 다음 칸에 들어간다 — 어디에 뭘 뒀는지 기억할 수 있어야 하기 때문. 단, 중간에서 아이템을 지우거나 짐싸기로 빼면 뒤의 아이템들이 한 칸씩 당겨 붙는다. 구멍은 남기지 않는다
4. **카테고리는 인벤토리마다 유저가 정하는 자유 태그다.** 추천 4종(옷장·냉장고·집·생활용품)은 이름과 기본 태그를 채워주는 지름길일 뿐, 저장된 뒤에는 직접 쓴 것과 구분하지 않는다. 인벤토리에 "종류(kind)"는 없다. 카테고리 칩의 맨 앞은 항상 **전체**이고, 태그가 없는 아이템은 전체에서만 보인다
5. **촬영은 모바일 전용이지만, 등록은 어디서나 된다.** 데스크톱에서는 카메라 대신 사진 업로드로 아이템을 등록한다. + 칸 · 플로팅 + 도 폰과 같은 원리다. 나머지 기능도 데스크톱에서 모두 동작한다 — 웹은 같은 기능을 넓게 펼쳐 놓은 것이지 기능이 더 있는 것이 아니다 (웹 그림에만 있는 것은 만들지 않는다)
6. **삭제는 물리 삭제가 아니라 `deletedAt` 기록이다.** 과거 INMYIN 게시물이 아이템을 참조하고 있다
7. **이미지는 원본과 배경제거본을 둘 다 보관한다.** 나중에 더 좋은 모델로 다시 따내기 위해서다
8. **플랜별 한도는 DB가 아니라 상수 테이블에서 읽는다.** `lib/plans.ts`

---

## 폴더 구조

```
app/
  (auth)/           로그인, 회원가입
  (main)/
    page.tsx        Home › 아이템
    inmyin/         Home › INMYIN
    discover/       Home › 발견
    u/[handle]/     타유저 프로필
    my/             My (프로필, 설정, 인벤토리)
    bookmark/       Bookmark 탭
    notifications/  알림 (Home 갈래 줄의 종)
  api/
components/
  ui/               버튼, 인풋 등 원자 단위
  inventory/        인벤토리 격자, 칸, 아이템 카드
  item/             아이템 등록 플로우
  inmyin/           캔버스 에디터 (Editor · Canvas(Konva) · ItemPicker · StickerSheet)
  heart/ bookmark/  하트 · 책갈피 버튼 (제목 줄의 [하트 · 하트 수 · 댓글 · 댓글 수 · 북마크])
  comment/          댓글 목록 + 입력칸, 제목 줄의 말풍선 · 댓글 수
  notification/     알림 줄, 본 시각 기록
lib/
  supabase/
  plans.ts          플랜별 한도 상수
  categories.ts     추천 인벤토리 4종의 이름과 기본 태그, 태그 한도
  background-removal.ts
  design-tokens.ts  globals.css 의 토큰을 서버에서 읽는다 — CSS 를 못 읽는 매니페스트 · 메타 태그용
  ranking.ts        인기·추천 정렬
  inmyin/           캔버스 저장 형태(canvas.ts) · 스티커 목록(stickers.ts)
scripts/
  app-icon.mjs      정해진 아이콘 SVG 로 PWA 아이콘 4종 + favicon 을 만든다 (npm run app-icon)
docs/
```

아이콘 시안을 폰에서 골라 보는 도구는 이 리포지토리 밖에 있다 — 옆 폴더 `../icon-preview` (어떤 프로젝트에서든 쓰는 도구라 따로 둔다).

PWA 파일: `app/manifest.ts`(매니페스트) · `app/sw.ts`(서비스워커) · `app/serwist/[path]/route.ts`(서비스워커를 내보내는 통로) · `app/offline/`(오프라인 화면). 서비스워커는 개발 중에는 꺼져 있다 — 확인은 `npm run build && npm start`.

---

## 작업 방식

- 한 번에 한 화면씩 만든다. `docs/roadmap.md`의 체크박스 하나가 한 작업 단위다
- 화면을 만들기 전에 `docs/screens.md`에서 그 화면의 정의를 읽는다
- 디자인 값(색·여백·폰트)은 피그마에서 옮겨온 Tailwind 토큰을 쓴다. 임의의 hex 값을 넣지 않는다
- **화면의 좌우 여백은 모든 화면에서 20px(`px-5`)이다.** 피그마에 화면마다 다른 값(25 · 28 · 44px 등)이 있어도 20px 로 맞춘다 — 폰에서 써 보고 정한 값이다. 뒤로 화살표처럼 64px 아이콘 칸 안에 그려진 그림은 칸을 밀어서(`-ml-1.25`) 보이는 끝이 20px 선에 오게 한다 (HeaderMini 가 이미 그렇게 한다)
- 반복되는 UI는 즉시 컴포넌트로 뺀다: 아이템 카드, 인벤토리 칸, 팔로우 버튼, 하단 탭
- 모바일 우선으로 짠다. 데스크톱은 같은 컴포넌트에 레이아웃만 넓힌다. 폰과 웹은 **1024px(`lg:`)** 에서 갈린다 — 웹은 하단 탭 대신 상단 메뉴(Home · My · Bookmark, 이름은 앱과 같게), 내용은 가운데 930px(`max-w-web`), 격자는 7칸 고정. 웹 그림이 없는 화면은 폰 모양을 가운데 좁게 놓는다
- 한 작업이 끝나면 `docs/roadmap.md`의 체크박스를 체크하고 커밋한다

### 이 프로젝트 주인은 개발이 처음이다

- 코드를 만들기 전에 **무엇을 만들 건지 먼저 말로 설명한다**
- 새 개념이나 라이브러리가 나오면 한 줄로 뜻을 풀어준다
- 선택지가 둘 이상이면 알아서 고르지 말고 장단점을 적어 물어본다
- 터미널 명령어는 무엇을 하는 명령인지 같이 적는다
- 본인이 직접 해야 하는 일(계정 만들기, 키 발급 등)은 클릭 순서까지 알려준다

---

## Next.js 버전 주의사항

Next.js 설치 도구가 만든 안내 파일을 함께 읽는다.

@AGENTS.md
